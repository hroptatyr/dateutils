changequote`'changequote([,])dnl
divert([-1])
changecom([##])

## we used to define YUCK_SCMVER_VERSION in m4 terms here
## however in C it's so much easier to come by with the same thing
## so we reserve this file for future (m4-based) goodness

divert[]dnl
changequote()dnl
